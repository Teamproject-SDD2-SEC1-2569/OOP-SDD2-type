version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/app # ซิงค์โค้ดในเครื่องกับ Container (แก้โค้ดแล้วอัปเดตทันที)